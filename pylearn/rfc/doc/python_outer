一. python与外部世界交互
	
	1. 命令行选项和参数 optparse
	2. 环境变量	os.environ
	
	3. 文件对象 
		open(name, mode, buff) -> a.加载， 磁盘到内存

			mode: r, w, a		默认文本模式打开，造成\r\n会转换成\n，所以打开二进制文件以b模式
				  (r/w)+		更新模式打开
				  U/rU			通用换行转换读

		f文件对象的操作	->	b.操作的是内存中的内容,与磁盘进行交互

		以yield形式产生输出		“增加了源和目的的交互次数，减少了内存分配"

	4. print
